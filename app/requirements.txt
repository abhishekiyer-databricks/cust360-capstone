# Databricks Apps runtime installs deps from THIS file (it does not read pyproject.toml).
# Keep in sync with pyproject.toml [project.dependencies]. Runtime = Python 3.11.
fastapi>=0.115
uvicorn[standard]>=0.30
databricks-sdk>=0.40
psycopg[binary,pool]>=3.2
httpx>=0.27
python-dotenv>=1.0
cachetools>=5.3
